<div class="mb-4">
    <div class="posts d-flex flex-row justify-content-between border border-dark rounded-top" >
        <h3 class="post-title p-2"><a href="/post/{{post.id}}">{{post.post_title}}</a></h3>
        <p class="post-user pt-3 pr-2" style="text-size: 10px; font-weight: bold;">Posted by {{this.user.username}} on {{post.created_at}}</p>
    </div>
    <div class="post-text border border-dark rounded-bottom p-2">{{post.post_text}}</div>
</div>